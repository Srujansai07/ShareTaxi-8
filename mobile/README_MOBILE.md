# ShareTaxi - Mobile App 📱

This directory contains the React Native / Expo mobile application for ShareTaxi.

## ⚠️ Environment Requirement

**You must have Node.js installed and added to your system PATH to run this project.**
The portable Node.js executable is insufficient for running Expo CLI.

## 🚀 How to Run

1.  **Install Node.js:** Download from [nodejs.org](https://nodejs.org/).
2.  **Install Dependencies:**
    ```bash
    cd mobile
    npm install
    ```
3.  **Start the App:**
    ```bash
    npx expo start
    ```
    - Press `w` to run in Web Browser.
    - Press `a` to run on Android Emulator.
    - Press `i` to run on iOS Simulator.
    - Scan the QR code with the Expo Go app on your physical device.

## 🛠️ Project Structure

-   `app/`: Contains the Expo Router pages.
-   `components/`: Reusable UI components.
-   `assets/`: Images and fonts.
